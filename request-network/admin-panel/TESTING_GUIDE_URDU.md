# 🧪 Admin Panel Frontend - راهنمای تست جامع

## ابتدا یاد بگیرید...

### ✅ وضعیت فعلی

```
Frontend (Next.js):    ✅ در حال اجرا (localhost:3000)
Backend (FastAPI):     ⏳ هنوز شروع نشده
Database:             ⏳ هنوز شروع نشده
Redis:                ⏳ هنوز شروع نشده
```

---

## 🚀 راه‌اندازی بک‌اند

### گام ۱: بک‌اند را اجرا کنید

```bash
# یک ترمینال جدید باز کنید
cd /workspaces/the_first/response-network/request-network/api

# بک‌اند را اجرا کنید
python main.py

# یا با uvicorn
uvicorn main:app --reload --port 8000
```

### گام ۲: دیتابیس را اجرا کنید

```bash
# با Docker (اگر موجود است)
docker compose up -d

# یا PostgreSQL را دستی اجرا کنید
```

### گام ۳: Redis را اجرا کنید

```bash
# Redis را اجرا کنید
redis-server
```

---

## 🧪 مراحل تست

### فاز ۱: بررسی صفحات

#### 1️⃣ صفحه ورود

```bash
# آدرس را باز کنید
http://localhost:3000/login

# بررسی کنید:
□ صفحه بارگذاری شود
□ فرم وجود داشته باشد
□ پس‌زمینه رنگی باشد
□ فیلدهای Username/Password وجود داشته باشد
□ دکمه ارسال وجود داشته باشد
□ سوییچ حالت تیره را ببینید
```

#### 2️⃣ صفحات داشبورد

```bash
# بعد از ورود
http://localhost:3000/dashboard

# در داشبورد:
□ ناوبری سایدبار را ببینید
□ خانه داشبورد را ببینید
□ شاخص‌های سلامت را ببینید
□ کارت‌های آمار را ببینید
□ دکمه خروج کار کند
```

#### 3️⃣ صفحات دیگر

```bash
# صفحه کاربران
http://localhost:3000/dashboard/users
□ لیست کاربران را ببینید
□ جستجو کار کند
□ مرتب‌سازی ستون‌ها کار کند

# صفحه درخواست‌ها
http://localhost:3000/dashboard/requests
□ لیست درخواست‌ها را ببینید
□ فیلترها کار کند
□ نوارهای پیشرفت را ببینید

# صفحه کش
http://localhost:3000/dashboard/cache
□ آمار کش را ببینید
□ دکمه پاک‌سازی کار کند
□ دکمه بهینه‌سازی کار کند

# صفحه تنظیمات
http://localhost:3000/dashboard/settings
□ انتخاب‌گر تم
□ سوییچ Auto-refresh
□ دکمه ذخیره
```

---

### فاز ۲: تست یکپارچگی API

#### تست کلاینت Axios

```typescript
// در کنسول مرورگر:

// ۱. به API متصل شوید
const response = await fetch('/api/health');
console.log(response);

// ۲. از بک‌اند تست کنید
curl -X GET http://localhost:8000/health
```

#### تست ورود

```bash
# با اطلاعات صحیح
curl -X POST http://localhost:8000/auth/login \
  -d "username=admin&password=admin" \
  -H "Content-Type: application/x-www-form-urlencoded"

# در کنسول مرورگر ببینید که توکن ذخیره شده باشد
console.log(document.cookie)
```

---

### فاز ۳: تست حالت تیره

```bash
# در هر صفحه:
1. آیکون ماه را بزنید (در هدر تنظیمات/داشبورد)
2. پس‌زمینه باید تغییر کند
3. رنگ متن تغییر کند
4. صفحه را رفرش کنید - تم باید حفظ شود
```

---

### فاز ۴: تست طراحی واکنش‌گرا

```bash
# در Chrome DevTools:
1. F12 را بزنید
2. Device Toolbar را فعال کنید (Ctrl+Shift+M)
3. اندازه‌های مختلف را تست کنید:
   □ موبایل (۳۷۵px)
   □ تبلت (۷۶۸px)
   □ دسکتاپ (۱۹۲۰px)

# بررسی کنید:
□ سایدبار واکنش‌گراست
□ کارت‌ها مجدداً چیده می‌شوند
□ جدول اسکرول افقی دارد
□ دکمه‌ها واکنش‌گرا هستند
```

---

### فاز ۵: تست مدیریت خطا

```bash
# بک‌اند را متوقف کنید:
1. بک‌اند را متوقف کنید (Ctrl+C)

# سپس تست کنید:
□ پیام خطا نمایش داده شود
□ اسپینر بارگذاری باشد
□ دکمه تلاش مجدد وجود داشته باشد

# بک‌اند را دوباره اجرا کنید:
python main.py
```

---

### فاز ۶: تست عملکرد

```bash
# در Chrome DevTools:
1. تب Network را باز کنید
2. صفحات را بارگذاری کنید
3. بررسی کنید:
   □ بارگذاری اولیه: < ۲ ثانیه
   □ فراخوانی‌های API: < ۵۰۰ms
   □ اندازه باندل: < ۲۰۰KB

# تب Performance:
1. ضبط را شروع کنید
2. صفحه را جابجا کنید
3. ضبط را متوقف کنید
4. معیارها را بررسی کنید
```

---

## 📋 چک‌لیست کامل تست

### تست‌های فرانت‌اند

- [ ] **همه صفحات بارگذاری شوند**
  - [ ] صفحه ورود
  - [ ] خانه داشبورد
  - [ ] صفحه کاربران
  - [ ] صفحه درخواست‌ها
  - [ ] صفحه کش
  - [ ] صفحه تنظیمات

- [ ] **ناوبری کار کند**
  - [ ] لینک‌های سایدبار
  - [ ] دکمه خروج
  - [ ] لینک لوگو/خانه

- [ ] **فرم‌ها کار کنند**
  - [ ] فرم ورود
  - [ ] فرم تنظیمات
  - [ ] جعبه‌های جستجو
  - [ ] منوهای فیلتر

- [ ] **UI/UX**
  - [ ] سوییچ حالت تیره
  - [ ] وضعیت‌های بارگذاری
  - [ ] پیام‌های خطا
  - [ ] پیام‌های موفقیت
  - [ ] انیمیشن‌ها روان

- [ ] **واکنش‌گرا**
  - [ ] نمای موبایل
  - [ ] نمای تبلت
  - [ ] نمای دسکتاپ
  - [ ] بدون اورفلو

### تست‌های یکپارچگی بک‌اند

- [ ] **اتصال API**
  - [ ] بک‌اند قابل دسترسی باشد
  - [ ] CORS فعال باشد
  - [ ] مدیریت تایم‌اوت

- [ ] **احراز هویت**
  - [ ] ورود موفق
  - [ ] توکن ذخیره شود
  - [ ] توکن در درخواست‌ها باشد
  - [ ] خروج توکن را پاک کند

- [ ] **بارگذاری داده**
  - [ ] داده داشبورد بارگذاری شود
  - [ ] لیست کاربران بارگذاری شود
  - [ ] لیست درخواست‌ها بارگذاری شود
  - [ ] آمار نمایش داده شود

- [ ] **مدیریت خطا**
  - [ ] مدیریت ۴۰۱
  - [ ] مدیریت ۴۰۳
  - [ ] خطاهای شبکه
  - [ ] تایم‌اوت‌های API

### تست‌های عملکرد

- [ ] **زمان بارگذاری**
  - [ ] صفحه اصلی < ۲ث
  - [ ] فراخوانی‌های API < ۵۰۰ms
  - [ ] انتقال‌ها روان

- [ ] **کنسول مرورگر**
  - [ ] بدون خطا
  - [ ] بدون هشدار
  - [ ] بدون منسوخ‌شدگی

- [ ] **حافظه**
  - [ ] بدون نشتی
  - [ ] پاکسازی هنگام unmount
  - [ ] مدیریت وضعیت بهینه

---

## 🔍 اشکال‌زدایی

### در کنسول مرورگر

```javascript
// وضعیت auth store را بررسی کنید
console.log(useAuthStore.getState());

// توکن را ببینید
console.log(document.cookie);

// کلاینت API را بررسی کنید
import { apiClient } from '@/lib/services/api-client';
console.log(apiClient);
```

### در تب Network

```
بررسی کنید:
- در هدر درخواست Authorization: Bearer {token}
- در هدر پاسخ CORS headers
- بدنه پاسخ JSON معتبر باشد
```

### پایش عملکرد

```javascript
// زمان بارگذاری صفحه
console.time('pageLoad');
// ... عملیات
console.timeEnd('pageLoad');

// زمان فراخوانی API
console.time('api');
const data = await fetch('/api/endpoint');
console.timeEnd('api');
```

---

## ⚠️ مشکلات رایج و راه‌حل‌ها

### مشکل: ورود انجام نمی‌شود

**راه‌حل:**
```
1. بررسی کنید بک‌اند در حال اجراست
2. بررسی کنید CORS فعال است
3. بررسی کنید اطلاعات ورود صحیح است
4. در تب Network پاسخ ۴۰۱ را ببینید
5. در لاگ‌های بک‌اند خطا را ببینید
```

### مشکل: داده بارگذاری نمی‌شود

**راه‌حل:**
```
1. بررسی کنید توکن ذخیره شده (کوکی)
2. بررسی کنید آدرس API صحیح است (.env.local)
3. بررسی کنید endpoint بک‌اند وجود دارد
4. بررسی کنید دیتابیس متصل است
```

### مشکل: حالت تیره ذخیره نمی‌شود

**راه‌حل:**
```
1. بررسی کنید localStorage فعال است
2. بررسی کنید کوکی‌های مرورگر فعال است
3. next-themes provider کار می‌کند
4. صفحه را رفرش کنید
```

### مشکل: واکنش‌گرا نیست

**راه‌حل:**
```
1. در DevTools viewport را ریست کنید
2. meta tag viewport را بررسی کنید
3. تنظیمات Tailwind را بررسی کنید
4. کش مرورگر را پاک کنید (Ctrl+Shift+Delete)
```

---

## 📊 نتایج مورد انتظار

### تست موفق فرانت‌اند

```
✅ همه صفحات با HTTP 200/307 پاسخ دهند
✅ صفحه ورود بدون خطا بارگذاری شود
✅ ناوبری سایدبار قابل مشاهده باشد
✅ سوییچ حالت تیره کار کند
✅ ارسال فرم‌ها کار کند
✅ بدون خطای کنسول
✅ طراحی واکنش‌گرا کار کند
```

### یکپارچگی موفق بک‌اند

```
✅ داده با موفقیت از بک‌اند بارگذاری شود
✅ توکن به درستی درج شود
✅ خطاهای ۴۰۱ به درستی مدیریت شود
✅ داده در UI به درستی نمایش داده شود
✅ جستجو/فیلتر کار کند
✅ به‌روزرسانی‌های بلادرنگ کار کند
```

---

## 🎯 ترتیب اجرای تست

```
1️⃣  فقط فرانت‌اند (بدون بک‌اند)
2️⃣  بک‌اند را اجرا کنید
3️⃣  تست اتصال API
4️⃣  جریان احراز هویت
5️⃣  تست بارگذاری داده
6️⃣  اعتبارسنجی UI/UX
7️⃣  تست عملکرد
8️⃣  تست مدیریت خطا
9️⃣  تست طراحی واکنش‌گرا
🔟 اعتبارسنجی نهایی
```

---

## ✅ تایید نهایی

**وقتی همه تست‌ها پاس شد این چک‌لیست را کامل کنید:**

- [ ] همه صفحات تست شد
- [ ] یکپارچگی بک‌اند کار می‌کند
- [ ] حالت تیره تایید شد
- [ ] طراحی واکنش‌گرا تایید شد
- [ ] مدیریت خطا تایید شد
- [ ] عملکرد قابل قبول است
- [ ] مستندات مرور شد
- [ ] آماده تولید

---

**آماده تست هستید! 🚀**

اگر مشکلی بود:
1. لاگ‌ها را ببینید
2. تب Network را بررسی کنید
3. مستندات را بخوانید
4. وضعیت بک‌اند را تایید کنید
